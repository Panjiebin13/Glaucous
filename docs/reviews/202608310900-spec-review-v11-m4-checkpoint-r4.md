# Spec 一致性评审报告：V1.1-M4 Checkpoint Spec（Git 快照 / 保留策略 / /rollback / 拒绝联动回退）

> 评审日期：2026-08-31 12:00
> 评审对象：`docs/designs/202608310900-plan-v11-m4-checkpoint.md`（状态：草稿）
> 对照文档：`docs/编程智能体需求文档v1.1.md`、`docs/编程智能体概要设计说明书v1.1.md`、`docs/Glaucous开发计划表v1.1.md`
> 模式：**聚焦复审（r4，非全量重评）**（改动范围：§3.3 空历史哨兵分句语义消歧（B6）+ §七 用例 7 补 count=0 清空断言（S12））
> 上一轮报告：`docs/reviews/202608310900-spec-review-v11-m4-checkpoint-r3.md`（不通过：阻塞 B6 + 建议 S12；B4/B5/S10/S11 已确认修复）
> 结论：**通过**（阻塞 0 项，建议 0 项）

## 一、评审范围（聚焦复审）

- 本轮仅复查 r3 遗留两项的修复及其波及面：**B6**（§3.3 L124 空历史哨兵分句——「（no-op）」括注改为「真实截断」语义后的最终表述）、**S12**（§七 用例 7 L216 补 count=0 + 空串锚清空对话断言）。
- 按用户复审要求，逐点验证 **§3.3 新表述 ↔ §二 哨兵说明 ↔ §四 实现 ↔ §七 用例 7/9** 四处一致性。
- 波及面重查（B6 触及 FR-42 上下文回退达成路径与概设 §5.1 核心机制，按规则无论章节是否改动均复查）：概设 §5.1「『同时回退上下文』= 截断 History 至该 checkpoint 对应轮次前」（本轮实际读取原文核对）、需求 §2.2 FR-42「任意 checkpoint」、需求 §4 裁剪底线「checkpoint 回退不可裁」（本轮实际读取原文核对）、决策 3「单一机制覆盖任意深度」、§3.4 调用点、§五 错误表 truncate 行。
- 未改动章节（任务 4.1~4.5 逐条落实、模块落位、B1~B5 修复内容、S1~S11 修复内容、§八 范围声明、头部要素与链接可达性）**继承上一轮结论**，不重复评审。
- 本轮为文档级修复，无代码改动，不涉及需求 §5 约束 6（git 子进程/零新依赖）的新增影响面。

## 〇、上轮阻塞项处置

| 编号 | 处置 | 说明 |
|---|---|---|
| B6 | **已修复** | §3.3 L124 哨兵分句由「仅当 `count == 0` 时合法（no-op）」改为：「`anchor_digest == ""` 为空历史首轮哨兵（B4），**仅允许与创建时 `message_count == 0` 的 checkpoint 配对**——**此时截断到 0 条 = 清空对话上下文（JSONL 仅 meta 行），是真实截断操作而非 no-op**（B6 消歧：与决策 3『单一机制』及概设『截断至对应轮次前』一致）」。双读法消除：本句与同句前半「绝不静默 no-op 或错位截断」中 no-op 的词汇含义（「操作不做任何事」）不再冲突，且与 L125 规范截断流程、决策 3、概设 §5.1 同向（处置详情见通过项表第 1 行）。r3 报告「存疑，提请作者确认」点已由落笔语义回应：作者本意确认为「真实截断」方向，与 r2/r3 既定修复语义一致。 |
| S12 | **已落实**（随 B6 顺带） | §七 用例 7 L216 补「**count=0 + 空串锚 → 清空对话（messages 空 + JSONL 仅 meta 行）**（S12）」，覆盖 r3 建议的核心断言（messages 为空 + JSONL 仅 meta 行），锁死「清空对话」语义不被将来改写为 no-op；同用例既有「re-load 等价」验证模式自然覆盖该分支（见通过项表第 4 行备注）。 |

## 二、阻塞问题

无。

## 三、建议问题

无。

## 四、通过项（仅本轮实际复查项；未列项继承上轮）

| # | 维度 | 检查要点 | 结果 |
|---|------|---------|------|
| 1 | 结构与可执行性 | **B6 修复本体**：§3.3 L124 新表述内部自洽——①与同句「绝不静默 no-op 或错位截断」词汇一致（清空对话是真实操作，非 no-op）；②与 L125 规范流程一致（校验公式 `count > 0` 才查锚 → count=0 天然通过 → 进入「内存截断 + JSONL 重写」→ 0 条 = 仅 meta 行）；③r3 指出的「提前返回 no-op」误实现路径已被显式排除（「是真实截断操作而非 no-op」），且该路径现被用例 7 断言兜底，误实现会被测试捕获 | ✓ |
| 2 | 结构与可执行性 | **四处一致性（用户指定验证点）**：①§3.3 L124「仅允许与创建时 message_count == 0 的 checkpoint 配对」↔ §二 L62「空历史首轮……为空串哨兵」——§四 创建逻辑保证 `anchor == "" ⟺ message_count == 0`（messages 为空 ⟺ len==0 ⟺ 锚为空串；非空历史锚为 sha256[:12] 恒 12 位非空），配对域互锁无第三态；②§3.3 ↔ §四 L161/L164——空历史首轮实际创建的 checkpoint 恰为 `(message_count=0, anchor="")`，是 §3.3 合法配对域的唯一来源，L158-160 注释「锚为空串哨兵」呼应；③§3.3 ↔ §七 用例 7 L216「count=0 + 空串锚 → 清空对话（messages 空 + JSONL 仅 meta 行）」逐字对应；④§3.3 ↔ §七 用例 9 L218「空历史首轮（新 History）anchor_digest 为空串且 create 成功」——创建侧不变量被测试锚定 | ✓ |
| 3 | 概设一致性 | **波及面**：概设 §5.1 L147-148（本轮实读原文）「『同时回退上下文』= 截断 History 至该 checkpoint 对应轮次前（JSONL 同步截断，配对校验复用 load 的修复逻辑）」——空历史首轮 checkpoint「对应轮次前」= 0 条消息 = 清空对话，§3.3 新表述显式引用该语义（「与概设『截断至对应轮次前』一致」），「配对校验」机制本体（ContextAnchorMismatch 显式拒绝路径）未变 | ✓ |
| 4 | 需求一致性 | **波及面**：FR-42 L70（本轮实读原文）「/rollback **可回退到保留期内任意 checkpoint**……提供『同时回退上下文』选项」——空历史首轮 checkpoint 属「任意」范围，清空对话语义使其上下文回退真实可达（r3 的 no-op 读法失效路径消除）；需求 §4 L139 裁剪底线（本轮实读原文）「checkpoint 回退」不可裁项达成路径无歧义。r3 通过项遗留注记「count=0 截断语义本身受 B6 影响」随 B6 修复解除 | ✓ |
| 5 | 结构与可执行性 | **S12 落实**：用例 7 L216 count=0 断言与 §3.3 L124「JSONL 仅 meta 行」表述同源，构成语义回归锚点；备注——S12 建议原文含「re-load 等价」，spec 未在 count=0 分句中显式重写该词，但用例 7 主断言句已确立「re-load 等价」为该用例一般验证模式，覆盖充分，不另立问题 | ✓ |
| 6 | 结构与可执行性 | **内部贯通抽查**：决策 3 L13「单一机制覆盖任意深度」（count=0 = 最深截断点，无分支例外）；§3.4 L134 调用点 `truncate_to(cp.message_count, cp.anchor_digest)`（空历史首轮 cp=(0,"") 正常清空、非空历史锚不匹配 → ContextAnchorMismatch 提示路径不变）；§五 L189「truncate 写失败 | 抛出」对 count=0 清空写同样适用。三处与新表述无矛盾 | ✓ |
| 7 | 继承上轮 | B4/B5 修复内容、S10/S11 修复内容、B1~B3、S1~S9、任务 4.1~4.5 逐条落实、模块落位、头部要素与链接可达、§八 范围声明 | 继承上轮（✓） |

## 五、复审要求

无。结论为**通过**：B6 与 S12 均已按上轮修复方向落实，四处（§3.3 / §二 / §四 / §七 用例 7、9）一致无矛盾，波及面（概设 §5.1、FR-42、裁剪底线、决策 3、§3.4、§五）复查无新问题。本 spec 评审闭环，可进入实现；无遗留待办（S12 备注中的「re-load 等价」断言措辞已判定覆盖充分，不构成登记项）。
