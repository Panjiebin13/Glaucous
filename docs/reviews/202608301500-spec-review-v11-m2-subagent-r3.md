# Spec 一致性评审报告：V1.1-M2 多 Agent 基础 Spec（spawn_agent + 子 AgentLoop + 权限继承 + 报告回传）—— r3 聚焦复审

> 评审日期：2026-08-30 17:20
> 模式：**聚焦复审**（改动范围：§4.1 + §六 默认值统一、§3.1 步骤 7 finally 哨兵恢复、§3.1 构造签名补 ctx、§8.3 冒烟文案、§5.1 引用指正、§2.1 modes 注释清理、§3.1 SubAgentInfo 注释同步、全文错别字「兑底→兜底」；其余章节继承 r2 结论，不做全量重评）
> 评审对象：`docs/designs/202608301500-plan-v11-m2-subagent.md`（第 3 轮；上一轮报告：`docs/reviews/202608301500-spec-review-v11-m2-subagent-r2.md`，结论不通过，B3/B4 阻塞 + S6~S9 建议）
> 对照文档：编程智能体需求文档v1.1（§2.5 FR-60~64、§4）、编程智能体概要设计说明书v1.1（§8.2/§8.3、§9）、Glaucous开发计划表v1.1（V1.1-M2 任务 2.1~2.5）
> 代码基线（本轮实际核对面）：`commands.py`（ReplContext D8 注记、_cmd_clear L248、_cmd_resume L266）、`cli.py`（confirm 闭包 L943~944）
> 结论：**通过**（阻塞 0 项，建议 0 项）

## 一、评审范围（聚焦复审）

按聚焦复审规则，本轮只复查任务声明的八处改动及其波及面；未改动且不涉波及的章节继承 r2「通过」结论。因 B3 修复触及维度二核心机制（权限管线归属字段、审计取值口径），波及面核查覆盖全文对 `"主 agent"` 字面与审计 `agent` 取值的全部出现点；因 B4 修复触及状态隔离核心机制（active_state 生命周期、confirm 闭包回退、D8 不变量），波及面核查覆盖「恢复/finally」全部出现点（§3.1 步骤 7、§4.4、§七、§8.1）并对照 commands.py、cli.py 实文复核。头部三份相对链接（编程智能体需求文档v1.1.md / 编程智能体概要设计说明书v1.1.md / Glaucous开发计划表v1.1.md）经解析全部实际可达（头部未改动，继承 r2 核查结论并复验文件存在）。

## 二、上轮阻塞项处置

| 编号 | 处置 | 核验依据 |
|---|---|---|
| B3 | **已修复（消除）** | §4.1（L175~176）现声明「`ApprovalAction` 增字段：`origin: str = "main"`、`origin_task: str = ""`（归属标注，**回调侧与测试消费**；取值口径与概设 §8.3 对齐）」「`ApprovalPipeline.__init__` 增参：`agent_label: str = "main"`、`agent_task: str = ""`」，与 §六（L229~230）「`origin: str = "main"`（gate 内 stamp，供回调侧/测试消费）」「`agent_label: str = "main"`」及审计行（L231）「取值 `main` / `child-<N>`，概设 §8.3」完全一致；消费方描述同步统一为「回调侧/测试消费」（r2 修复方向两项要求均落实）。上游实文吻合：概设v1.1 §8.3 L291「audit.log 每条记录增加 `agent` 字段（`main` / `child-1`）」。全文 `"主 agent"` 字面仅剩 5 处（L14/L139/L182/L195/L232），全部为 `ReplContext.active_agent`（UI 归属标签）语境，且 §3.1 步骤 7 恢复值、§4.2 卡条件、§4.4/§六 字段默认值四处彼此一致——UI 标签「主 agent」与审计取值「main」双体系边界清晰，无混淆残留。r2-B3 的矛盾面（§4.1 vs §六/决策 4/§8.1/概设 §8.3）已全部闭合 |
| B4 | **已修复（消除）** | §3.1 步骤 7（L139）现声明「finally 恢复为哨兵：`ctx.active_state = None` / `active_agent = "主 agent"` / `active_task = ""`」，并附修复注记明确「恢复 None 哨兵而非 ctx.state 实例——实例值会在 /clear、/resume 整体替换 state 后变成旧对象，违反 D8；None 语义下 confirm 闭包动态回退 ctx.state，永远读到最新实例」。与 §4.4（L195）「`active_state: SessionState \| None = None`（None 语义 = 等于 `ctx.state`，属性访问处统一回退）」、（L196）「改读 `ctx.active_state or ctx.state`……active_state 为 None 回退原字段」哨兵语义完全一致；§七（L250）「不残留子任务值」、§8.1（L263）「runner 返回后 ctx.active_state/active_agent/active_task 已恢复」随之自洽。代码事实复核成立：commands.py L93~94「/clear、/resume 会整体替换 history/state/loop/pipeline；命令层与回调层一律经本对象间接引用（D8：闭包不捕获旧对象）」、L248 `ctx.state = SessionState()`（/clear）、L266 `ctx.state = state`（/resume）均为整体替换且不触碰 active_state；cli.py L943~944 confirm 闭包现为 `ctx.state.mode == MODE_PLAN` 动态属性访问，改读 `ctx.active_state or ctx.state` 无需重构。r2 列举的缺陷序列（spawn→/clear 或 /resume→submit_plan→confirm 读旧实例）已从根上消除，D8 与 FR-39 出口（planning.py「PLAN→Build 唯一工具面出口」）均守住。全文「恢复/finally」共 6 处，无一处再要求恢复为实例值 |

### 上轮建议项落实

| 编号 | 处置 |
|---|---|
| S6 | **已落实**：§8.3（L278）冒烟文案改为「标注「🕊 子 agent（任务：…）」（概设 §8.3 口径）；audit.log 中子 agent 事件含 `agent: child-1` 归属字段」。全文 🪽 与「父任务：」卡面字面零残留（决策 4 的「{父任务摘要 ≤40 字}」为占位符描述，与概设 §8.3「`🕊 子 agent（任务：{父任务摘要}）`」占位符措辞一致，非卡面字面）。与概设 §8.3 L290 卡片标题、L301「🕊 子 agent 出发」意象对齐 |
| S7 | **已落实**：§3.1 签名块（L122）补列 `ctx: "ReplContext",  # 归属切换载体（active_state/active_agent/active_task）`，与步骤 7（L140）「构造参 `ctx: ReplContext`，接线时传入」同步，签名与正文不再脱节 |
| S8 | **已落实**：§5.1（L212）「测试固化（§8.1）」，指向真实用例位置（§8.1 L258「断言父 History 长度增量恰为 2（assistant+tool）」），交叉引用失准消除 |
| S9 | **已落实**：§2.1（L84）modes 注释改为「子 agent 模式取派发时父模式快照（独立副本，§4.3）」，「共享 state」残留清除；全文「共享」仅剩 3 处且均为合法语境（决策 1「放弃共享实例方案」、L112「绝不共享实例」、L135「共享工具对象」），不再与决策 1/§3.1 步骤 3/§4.3 冲突 |

## 三、阻塞问题

无。

## 四、建议问题

无。

## 五、通过项（本轮实际复查）

| 维度 | 检查要点 | 结果 |
|------|---------|------|
| 概设一致性 | B3 修复复核（权限体系核心机制）：§4.1/§六 默认值统一 `"main"` 后，`_event()` 直取 `agent_label` 的主 agent 审计值 = `main`，与概设 §8.3 L291「agent 字段（main / child-1）」、决策 4（L14）、§8.1（L261）断言四点一线；origin 消费方描述两处统一为「回调侧/测试消费」，与 §4.2 审批卡实际读 `ctx.active_agent`（UI 通道）的分工不再矛盾 | ✓ |
| 概设一致性 | B4 修复复核（状态隔离核心机制）：None 哨兵恢复与 §4.4 哨兵语义、commands.py D8 注记及 /clear（L248）/resume（L266）整体替换实文、cli.py L943~944 confirm 闭包现实现全部相容；/clear、/replace 波及缺陷路径消除，FR-39 出口保持可达 | ✓ |
| 结构与可执行性 | 波及面核查——「恢复/finally」6 处（L27 无关、L136/L139/L197/L250/L263）全部与哨兵语义一致，无实例值恢复残留；「"主 agent"」5 处全部为 active_agent UI 标签语境且四处声明互洽；无新矛盾 | ✓ |
| 结构与可执行性 | S7 复核：签名块含 `ctx: "ReplContext"`（L122）+ 注释与步骤 7（L140）一致；§九 cli.py 接线行由「runner 组装」涵盖 ctx 传入（§3.1 L140「接线时传入」为准），无冲突 | ✓ |
| 结构与可执行性 | S6 复核：§8.3 冒烟文案（L278）与决策 4、§4.2 卡面格式、§8.1 child-1、概设 §8.3/§9 逐字对齐；🪽/「父任务：」字面零残留 | ✓ |
| 结构与可执行性 | S8 复核：§5.1 引用（§8.1）指向真实断言位置 | ✓ |
| 结构与可执行性 | S9 复核：§2.1 注释与决策 1/§3.1 步骤 3/§4.3 机制声明一致，引用 §4.3 有效（快照复制语义所在章节） | ✓ |
| 结构与可执行性 | 改动 7/8 复核：SubAgentInfo.agent_id 注释「child-<N>」（L103）与 L131 分配规则、§六 L228、§8.1/§8.3 取值同源；「兑底」全文 0 匹配，兜底表述（L130/L149/L243）语义正常 | ✓ |
| 需求一致性 | 硬约束波及面复查：本轮改动均为字段默认值/恢复语义/文案/引用/注释层面，不触及需求 §4/§5 硬约束（截止时间、提交物、合规项）约束面，无新增违反（继承 r1/r2 通过结论） | ✓ |
| 需求一致性 | FR-62 归属链路复核：审计 `agent`（main/child-<N>）+ `agent_task`、审批/方案/提问三类卡归属标注、快照继承不回流——B3 修复后整链取值口径闭合（概设 §8.3 对齐） | ✓ |
| 其余章节 | 决策记录 1~3、§一 架构图、§二 工具契约、§3.2/3.3、§4.2/4.3、§5.2、§8.2、§九——未改动且不涉本轮波及面 | 继承上轮 ✓ |

## 六、复审要求

**结论：通过**——B3/B4 已确认消除，S6~S9 及两项附带同步（改动 7/8）全部落实，未引入新的不一致。无阻塞项、无建议项，无待登记事项。

本 spec 可按 §九 实现位置汇总进入 V1.1-M2 任务 2.1~2.5 的实现阶段。
