# Spec 一致性评审报告（第 r5 轮·聚焦复审）：v1.1 前置产品化打磨（技能创建 / 补全增强 / 思考折叠 / 用量展示 / 交互升级）

> 评审日期：2026-08-29 19:40
> 评审对象：`docs/designs/202608291800-plan-v11-productization-polish.md`（提交 517693c）
> 上轮报告：`docs/reviews/202608291800-spec-review-v11-productization-polish-r4.md`（不覆盖，本报告为增量）
> 模式：聚焦复审（改动范围：§3.1 / §3.3 / §3.4、§九、§十.3、§十一.3；连带波及 §3.2 缓冲语义注记、§5.2 口径、文档头）
> 对照文档：`docs/编程智能体需求文档.md`（§4/§5 硬约束）、`docs/编程智能体概要设计说明书.md` §5/§10、代码现状（cli.py、commands.py）、r1~r4 历轮报告
> 结论：**有条件通过**（阻塞 0 项，建议 2 项）

## 一、评审范围

按本轮任务聚焦执行：① 逐处核对 r4-B1（N 口径同步）与 r4-B2（缓冲轮末保留、下轮开始清空）闭合；② 对新时序做场景推演（折叠开启轮间隙回看、管道模式、异常路径）与波及面复查（stream_end 残留、「下一轮开始清空」与 /clear、/resume 显式清空的时序关系、turn_usage 清空时机），均比对代码现状；③ 全文一致性快扫（时序/口径/引用编号）。未改动章节（§一、§二、§四、§5.1、§5.3、§6.1、§6.2、§七、§八、§十 其余测试项、§十一 其余条目）继承 r4 结论，不重复评审。

## 二、上轮问题关闭情况

| 编号 | 状态 | 证据（v1.1 当前位置 × 核验事实） |
|---|---|---|
| r4-B1 §3.1 N 定义与 §3.4/§十.3 统一口径不能同时成立 | ✅ 已关闭 | §3.1 已改为「**N = 思考区已收纳条目总数 = 非 text 事件 + 交互伪事件**，与缓冲、/expand、§3.4 同一口径（r4-B1 修复）」，即按上轮修复方向①落实；§3.2 缓冲范围 ①+②（「① 非 text 事件（text 增量不缓冲，与思考区收纳范围一致）；② 交互伪事件」）与 N 口径一致，且「思考区只计①」的旧界定已删除；§3.4 不变量「摘要行的 N 与缓冲条目数一致（两者同为「非 text 事件 + 交互伪事件」口径）」；§十.3「摘要行 N 与缓冲条目数一致性」。四处口径逐一比对一致，含审批/提问交互轮次的 N 与 /expand 条数可同时验收。Live 顶行在 pause（阻塞交互）期间计数、resume 后刷新可由 §3.2 pause=Live.stop()/resume=Live.start() 语义直接推出，无实现歧义 |
| r4-B2 轮末清空致 /expand 永空 | ✅ 已关闭 | 新时序四处落实一致：§3.1 步骤 3「缓冲**不在轮末清空**（r4-B2 修复）：turn_events/turn_usage 保留为「上一轮」数据，直到下一轮任务开始时由 repl 清空重置」；§3.1 /expand「缓冲保留至下一轮开始，间隙内均可回看」；§3.2 turn_events 语义注记同步为「轮末保留、下轮开始时重置」；§3.3「缓冲重置时机（r4-B2）：每轮任务开始时（repl 拿到非斜杠输入、调 run() 之前）调 reset_turn_buffers」；§十.3「缓冲重置时机（下轮开始时清空而非轮末）、/expand 分支（间隙回看 / 空缓冲提示）」；§十一.3「在输入下一个任务前执行 /expand 可回看上一轮思考过程（含交互记录）」。**场景推演**：① 折叠开启轮——轮末收缩后缓冲在，/expand 可回看；输入下一任务（非斜杠）→ run() 前清空 → 执行，正确；② 管道模式——事件实时打印 + turn_events 仍记录（§3.3「管道下 /expand 可用」），间隙可回看，正确；③ 异常路径——§3.3 finally 执行步骤 1~3「缓冲按步骤 3 保留供回看」，与 §十.3「mock run 抛错断言缓冲保留」互证。代码核验：cli.py repl 为单线程输入循环（L717 `while True`，斜杠命令 L743-747 分派后 `continue`，`run()` L751 期间输入被阻塞），/expand 只可能在轮间间隙被分派——新时序下该窗口内缓冲非空，回看真实可达 |

**上轮阻塞项处置**：r4-B1 已修复；r4-B2 已修复。

## 三、新增阻塞问题

无。

## 四、新增建议问题

### S1. /expand 空缓冲触发条件枚举不完备且含不可达分支
- **维度**：结构与可执行性（内部自洽·表述精度）
- **位置与摘录**：§3.1 /expand 段「缓冲为空（启动后首轮未跑任务、或已开启下一轮）提示『暂无可展开的思考过程』」。代码事实：`run()` 执行期间用户输入被阻塞（cli.py L751），/expand 只可能在轮间分派，「已开启下一轮」不可被用户观测；而 §3.3 明示 /clear、/resume 也显式调 `reset_turn_buffers`，这两条真实可达的空缓冲路径未列入枚举。
- **建议**：将枚举修订为实际可达条件（启动后首轮未跑任务、/clear 或 /resume 之后），删除或改写不可达的「已开启下一轮」分支。不影响实现判断，登记待办即可。

### S2. 文档头「评审修复记录」行未登记 r4 吸收
- **维度**：结构与可执行性（头部要素·追溯性）
- **位置与摘录**：文档头第 8 行「评审修复记录：首轮（B1~B4/S1~S10）、r2 复审（r2-B1/r2-B2/r2-S1~S4）、r3 复审（r3-B1/S1/S2……）已全部吸收」——正文已按 r4 修复（§3.1 标题及各处「r4-B1/r4-B2 修复」标注），但头部追溯行止步于 r3。
- **建议**：头部修复记录行补记「r4 复审（r4-B1/r4-B2）已吸收」，与正文标注保持同步。

## 五、通过项（本轮实际复查）

| 维度 | 检查要点 | 结果 |
|------|---------|------|
| 结构与可执行性 | r4-B1 闭合：§3.1 N 定义、§3.2 缓冲范围、§3.4 不变量、§十.3 断言四处口径逐一比对一致（均为「非 text 事件 + 交互伪事件」） | ✓ |
| 结构与可执行性 | r4-B2 闭合：轮末保留 → 下轮开始（非斜杠输入、run() 前）清空的新时序在 §3.1/§3.2/§3.3/§十.3/§十一.3 五处一致；折叠开启轮、管道模式、异常路径三场景推演均成立，/expand 间隙回看真实可达（代码核验 repl 单线程分派） | ✓ |
| 结构与可执行性 | §3.1 摘要行「— /expand 查看」提示附「提示有效——缓冲保留至下一轮开始，间隙内 /expand 可回看，见步骤 3 与 3.3」，与新语义一致；§十一.3 验收文案同步为「在输入下一个任务前执行 /expand 可回看」；全文检索无「折叠开启不可展开」「本轮暂无」类旧文案残留（空提示已改为「暂无可展开的思考过程」） | ✓ |
| 结构与可执行性 | 波及面①（零改动声明）：全文检索 `stream_end` 仅两处合法出现——§3.1 标题「无 stream_end 新事件」与既有事实段「原 stream_end 事件在轮末锚定后已无消费方，删除，loop 零改动」（删除说明）；§5.2、§七（「不再以事件为锚点」）、§九 R7 行（「轮末（run 返回后）接线」）、§九 R3 行（「loop 零改动」）均无锚点残留，与「不新增任何 loop 事件」声明自洽 | ✓ |
| 结构与可执行性 | 波及面②（清空时序竞争）：「下一轮任务开始时清空」钩子为非斜杠输入后、run() 前（代码核验该位置存在且唯一）；/clear、/resume 显式清空发生在各自的命令分派内（commands.py L150-185，rebuild_loop 前）——repl 单线程顺序执行，两路径无并发竞争；下轮开始再次清空为幂等操作，重复声明不构成矛盾，且 S3 理由（ctx 同一对象复用、字段不随 rebuild_loop 自然重置）经代码核验仍成立 | ✓ |
| 结构与可执行性 | 波及面③（turn_usage 清空时机）：§3.1 步骤 3 明示 turn_events/turn_usage 一并保留至下一轮开始清空；§3.3 的 reset_turn_buffers 与该时机同一钩子；§5.2「turn_usage 只累计任务轮内（run() 调用期间）」与「清空先于 run()、累计始于 run() 内」自洽，轮末摘要行 token 段与用量行同源（turn_usage）不受影响。§5.2 未独立复述清空时机，由 §3.1/§3.3 覆盖，不构成歧义（若求精确可在 §5.2 补一句交叉引用，属文字优化非缺陷） | ✓ |
| 结构与可执行性 | 全文一致性快扫：§3.1 步骤 1~3 编号与文内「见步骤 3 与 3.3」引用一致；§5.2 轮末渲染顺序（摘要行 → md 卡片 → 用量行）与 §3.1 步骤 1、2 一致；§3.3 异常路径「跳过步骤 2 卡片渲染、用量行遵循 §5.3」与 §3.1/§5.3 自洽；§十 验收基线冒烟含 /expand；§九 各行文件归属与正文章节一致 | ✓ |
| 需求一致性 | 硬约束（需求文档 §4/§5）与范围蔓延：本轮修复仅调整缓冲生命周期与口径表述，未触碰凭据零存储、截止期、提交物安排，未引入新需求；/expand 回看恢复真实可用，头部决策记录「思考折叠用 /expand 展开」的用户决策价值落地 | ✓ |

其余未改动章节（§一、§二、§四、§5.1、§5.3、§6.1、§6.2、§七、§八、§十.1/2/4/5/6/7、§十一 其余条目及首轮~r4 相应结论）**继承上轮**，本轮不重复评审。

## 六、复审要求

有条件通过，无阻塞项。建议项登记去向：

- **S1**（/expand 空缓冲条件枚举）与 **S2**（头部修复记录补记 r4）均为表述精度/追溯性问题，不影响开工与实现判断，登记为 v1.1 实现期的文字修订待办即可，无需再启评审轮次；若作者希望文档零瑕疵，可在下次提交顺手修订后由常规抽查确认。
- r1~r4 历轮全部阻塞项（B1~B4、r2-B1/B2、r3-B1、r4-B1/B2）经本轮及历轮复核均已闭合，本文档可作为 v1.1 开发前的最终产品化打磨依据放行。
