# Spec 一致性评审报告：V1.1-M3 会话管理 Spec（r4 聚焦复审）

> 评审日期：2026-08-30 23:59
> 评审对象：`docs/designs/202608302000-plan-v11-m3-sessions.md`
> 模式：**聚焦复审（r4，非全量重评）**——改动范围：§七「新建会话唯一入口」行（B5）、§5.4 引言行签名与 §十 commands.py 行（S14）、§5.1 步骤 3（S15）、§4.4 未标注桶示例与 §9.2 测试输入表述（S16）；其余未改动章节继承 r1/r2/r3 结论
> 上一轮报告：`docs/reviews/202608302000-spec-review-v11-m3-sessions-r3.md`（结论：不通过，B5 阻塞 + S14~S16 建议；B1~B4 已确认消除）
> 对照文档：编程智能体需求文档v1.1（§2.3 FR-44~51、§4）、编程智能体概要设计说明书v1.1（§6、§8.3）、Glaucous开发计划表v1.1（V1.1-M3 任务 3.1~3.6）
> 代码基线：`src/glaucous/cli.py`（resume_history L1187~1211 / repl L1457~1460）、`commands.py`（_cmd_clear L253）、`permission/approval.py`（record_denial L165~179 / _event L183~202）、`agent/subagent.py`（L170）
> 结论：**有条件通过**（阻塞 0 项，建议 1 项）

## 一、上轮阻塞项处置

- **B5 —— 已修复**。§七「新建会话唯一入口」行现为「`create_session_history(system_prompt, workspace) -> (History, degraded)`（覆盖 repl/resume **兜底 ×3**/_cmd_clear **共 5 处**）」：「共 4 处」→「共 5 处」、「兑底」→「兜底 ×3」（×3 比 r3 修复方向要求的「兜底」更精确，与 §5.4/§一 既有表述对齐）。**残留清零验证**：全文「兑底」「共 4 处」0 匹配。**代码逐处复核**（波及面规则：FR-44 收敛覆盖属需求核心项，本轮重查）：cli.py L1190（未找到可恢复的会话）/ L1198（未找到会话 \<id\>）/ L1205（恢复失败）三处兜底 + L1460（repl 新会话）+ commands.py L253（_cmd_clear）= 主会话创建 **5 处**；subagent.py L170 走 `subdir="agents"` 属决策 6 排除范围不计入——与修订后 §七 完全吻合。

## 二、上轮建议项（S14~S16）落实核对

| 项 | 结论 | 核对依据 |
|---|---|---|
| S14 | **部分落实**（声明范围两项全部落实；r3 建议第三点未纳入本轮 → 残留立建议 S1） | ① §5.4 引言行已改「`create_session_history(system_prompt, workspace) -> tuple[History, bool]`」，与同节代码块签名一致（裸「-> History」全文 0 匹配）；② §十 commands.py 行已补「_cmd_clear 换 create_session_history 统一入口」，与 §一 分层影响表状态层行、§5.4 收敛表 /clear 行、§9.3 fake 适配条同步。③ r3 建议中的「§一 流程图同步（或登记 TODO）」未纳入本轮修复声明范围：§一 流程图仍写「History.create(session_dir=project_dir(workspace)) 或 resume（用户级目录）」，未同步亦未登记 TODO——按聚焦复审规则该行未改动、继承 r3 定性，但 S14 作为整体未闭环，残留如实记为建议 S1 |
| S15 | **已落实** | §5.1 步骤 3 现为「其内部**三处兜底 return 分支**（未找到/前缀未命中/恢复失败）也统一走 §5.4 入口」——数量与枚举顺序与 §5.4「兜底实为 3 处——未找到/前缀未命中/恢复失败，cli.py L1190/L1198/L1205」及收敛表行完全一致，与代码三处 return 分支逐一对应 |
| S16 | **已落实** | ① §4.4 现为「无 agent 字段的行归入「未标注」桶（r2-S11，如 **record_denial 产出的 plan_mode_blocked 行——审批管线格式但无 agent 字段**）」——代码核实：record_denial（approval.py L167~178）行含 `decision: "plan_mode_blocked"`、无 agent 字段；_event（L183~202）恒有 agent（L199）。术语已与决策 7 的「审批管线行 / 命令审计行（at/event）」分类对齐，且显式承认 agent 字段为 v1.1-M2 增量（概设 §8.3「audit.log 每条记录增加 agent 字段」）的特例，与 r2-S11 原定位一致；② §9.2 现为「audit.log 含 at/event 格式行（无 decision）→ 不计入决策分布；**「有 decision 无 agent」的行（如 plan_mode_blocked）→ 归「未标注」桶**」——两个断言的输入拆清，与 §八「行无 decision 字段……跳过不统计」的过滤顺序不再有误读空间 |

## 三、阻塞问题

无。

## 四、建议问题

### S1. §一 流程图仍写旧建会话表述——r3-S14 第三点未随本轮收口同步或登记 TODO
- **维度**：结构与可执行性（表述不同步；有权威替代，误导性低——r3 已定性）
- **位置与摘录**：§一 流程图「启动：load_config → migrate_legacy_sessions(workspace)（FR-51，幂等）→ `History.create(session_dir=project_dir(workspace))` 或 resume（用户级目录）」；对照本轮已收口的统一入口口径四处：§5.4 引言行/代码块（`tuple[History, bool]`）、§5.1 步骤 2（「经**统一存储入口**……覆盖全部 5 处新建会话调用点」）、§七（「覆盖 …… 共 5 处」）、§十（「_cmd_clear 换 create_session_history 统一入口」）
- **说明**：r3-S14 建议原文为「§一 流程图与 §十 随下次修订一并同步（或登记 TODO 注明去向）」，本轮声明修复范围仅含 §5.4 引言行与 §十 两项；§一 流程图未改亦未登记 TODO，S14 整体未闭环故残留立项。定性维持 r3：「可读作统一入口的内部机制（create_session_history 内部即调用 History.create 并传 session_dir），误导性低」——且本轮四处权威口径收口后，该行被进一步孤立，不影响实现判断（实现者按 §5.4 收敛表执行），不构成阻塞。
- **建议**：随下次修订顺带同步一行（方向：改提「经 create_session_history（§5.4）」或注明流程图行为统一入口内部机制）；如继续暂缓，按 r3 复审要求登记 TODO.md 并注明去向。

## 五、通过项（本轮实际复查；未列项继承 r1/r2/r3 结论）

| 维度 | 检查要点 | 结果 |
|------|---------|------|
| 需求一致性 | FR-44 收敛覆盖（波及面重查）：§七「共 5 处 / 兜底 ×3」与 §5.4（5 处 / 兜底 ×3 / L1190/L1198/L1205）、§一 分层影响表、§5.1 步骤 2 四处口径一致；与代码逐处核实一致（cli.py L1190/L1198/L1205/L1460 + commands.py L253；subagent.py L170 属决策 6 排除） | ✓ |
| 需求一致性 | FR-49 统计口径：§4.4「未标注」桶承接概设 §8.3 agent 字段为 v1.1-M2 增量的事实（record_denial 早于该机制无 agent），与决策 7「只统计含 decision 的行」+ §八 过滤顺序自洽闭环 | ✓ |
| 结构与可执行性 | create_session_history 签名口径全文一致：§5.4 引言行 = 代码块（`tuple[History, bool]`）、§5.1 步骤 2（二元组解包）、§七（`(History, degraded)` 简写，r3 已接受）、§八/§9.2（degraded=True 回退路径） | ✓ |
| 结构与可执行性 | §5.1 步骤 3 与 §5.2 衔接无矛盾：三处兜底分支枚举与 §5.4 收敛表、代码三处 return 分支一致；find_latest_session/--resume 改造描述不受影响 | ✓ |
| 结构与可执行性 | §十 commands.py 行与 §一 分层影响表、§5.4 收敛表 /clear 行、§9.3 fake 适配条四处同步 | ✓ |
| 结构与可执行性 | §9.2 测试输入两断言与 §4.1 ③ / §3.2 prefix_candidates、§4.4 未标注桶、§八 过滤顺序对应完整 | ✓ |
| 结构与可执行性 | 波及面：旧残留清零（「兑底」「共 4 处」、裸「-> History」全文 0 匹配）；§六 stats.py 纯函数按 decision 聚合与 §4.4 命令层 agent 维度归组分层不冲突；概设 v1.1 §6 /stats 描述、§8.3 agent 字段声明相符 | ✓ |
| 结构与可执行性 | 头部「关联规格」三链接可达（编程智能体需求文档v1.1.md / 编程智能体概要设计说明书v1.1.md / Glaucous开发计划表v1.1.md 均实际存在）；头部要素未改动 | ✓ |

**继承上轮（未改动且未被波及）**：硬约束合规（需求 §4/§5 截止时间/提交物/合规项）、FR-45~48/50/51 覆盖与计划表任务 3.1~3.6 对应、/fork 收窄裁剪合规（FR-48 收窄确认）、范围守恒（无蔓延/无静默遗漏）、决策 1/2/3/4/5/6/7 已知边界、§3.1 字段集与概设 §6.1 超集表述、§3.2 消解规则闭环、§4.1/4.2/4.3/4.5/4.6/§5.2/§5.3/§六/§九其余条目——按 r1/r2/r3 通过项结论继承。

## 六、复审要求

**结论：有条件通过**——B5 已修复到位，S14~S16 声明范围全部落实，无阻塞项，放行。

1. 建议项 S1 不阻塞放行：请登记至 TODO.md 并注明去向，或随下次修订顺带同步 §一 流程图一行（成本最低）。
2. 本轮四项修复（B5/S14/S15/S16）质量良好：均为声明范围内行级改动，未引入新矛盾，未触及机制变更。

复审时按规范生成新报告文件，不覆盖本报告。
