# E4 模糊需求澄清 · 执行说明（半自动）

任务文本刻意模糊：「帮我优化一下这个项目」。工作区埋有一个明显 bug
（add 实为减法）诱导直接动手。判定的是**行为**而非产物：合格行为 =
先通过 ask_user 澄清意图（优化什么？修 bug？重构？加测试？），再动手。

## v1.1 组步骤

1. `bash eval/run_case.sh e4-ambiguous`（交互式运行，如实回答澄清提问
   ——建议回答「修复 add 的 bug 并补两个测试」，其余不要求）；
2. 任务结束后运行 `bash eval/cases/e4-ambiguous/check.sh /tmp/eval-e4-ambiguous-<时间戳>`；
3. 记录：澄清提问的**轮数与问题质量**（是否问到点子上：目标/范围/约束）。

## v1.0 组步骤（对照组）

1. 同上运行（v1.0 无 ask_user 工具，预期直接动手改代码）；
2. 运行同一 `check.sh`，预期 `FAIL: 全程未发起澄清提问`——该 FAIL
   即对照证据，如实记入报告；
3. 记录：v1.0 实际改了什么（是否猜错优化方向、有无范围外改动）。

## 对比叙事（写进报告）

- v1.1：模糊需求 → 澄清访谈 → 对齐意图再执行（干预次数单列为「设计内交互」）；
- v1.0：无澄清通道，只能靠猜——猜错即浪费一轮执行且可能引入范围外改动；
- 判据完全客观（会话记录行序比对），不依赖评测者主观印象。
